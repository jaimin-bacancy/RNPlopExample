import styles from './{{pascalCase screen}}.styles';
import React from 'react';
import { View, Text } from 'react-native';

export function {{pascalCase screen}}() {
  return (
    <View style={styles.container}>
      <Text>{{pascalCase screen}}</Text>
    </View>
  );
}
